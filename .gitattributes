# Normalise line endings: LF in the repository, native in the working copy.
# Keeps diffs clean when the project is cloned on Linux/macOS for the stage 5
# and 6 backends.
* text=auto eol=lf

*.sln text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

*.ico binary
*.png binary
